export const environment = {
  production: true,
  // ⚠️  Set secret RAILWAY_API_URL (e.g. https://xxx.up.railway.app) in:
  //     GitHub repo → Settings → Secrets and variables → Actions
  apiUrl: 'https://RAILWAY_API_URL/api/zakat'
};
