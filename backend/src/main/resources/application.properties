server.port=${PORT:8080}
spring.application.name=zakat-simulator

# Nisab values (updated periodically)
zakat.nisab.gold.grams=85
zakat.nisab.silver.grams=595
zakat.rate=0.025

# CORS — override via CORS_ALLOWED_ORIGINS env variable in Railway
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:4201,https://aya-fekih-romdhane.github.io}
